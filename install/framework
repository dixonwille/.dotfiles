#!/bin/env bash
packages=(
  bin
  hyprland
  ghostty
  git
  nvim
  oh-my-posh
  tmux
  zsh
  zsh-plugins
  personal/git
  personal/jj
  personal/ssh
)

PKGS="${packages[@]}" ./install/install

ln -sf "/opt/1Password/op-ssh-sign" "$HOME/.local/bin/op-ssh-sign"

sudo stow -Rt /etc regreet
